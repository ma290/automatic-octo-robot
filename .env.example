# SQLite (default for development and Docker)
DATABASE_URL="file:./data/estate-plus.db"

# PostgreSQL (swap this in for production scaling)
# DATABASE_URL="postgresql://user:password@localhost:5432/estateplus?schema=public"
